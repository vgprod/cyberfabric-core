{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gts://gtx.cf.chat_engine.session.create_request.v1~",
  "title": "SessionCreateRequest",
  "description": "Request to create a new chat session. tenant_id, user_id, and client_id are extracted from the JWT bearer token and must not be supplied in the request body.",
  "type": "object",
  "properties": {
    "session_type_id": {
      "$ref": "../common/GtsSessionTypeId.json",
      "description": "ID of the session type to use for this session"
    },
    "metadata": {
      "type": "object",
      "description": "Optional initial session metadata",
      "additionalProperties": true
    }
  },
  "required": ["session_type_id"],
  "additionalProperties": false
}