{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gts://gtx.cf.chat_engine.session.search_request.v1~",
  "title": "SessionSearchRequest",
  "description": "Request to search messages within a specific session",
  "type": "object",
  "properties": {
    "session_id": {
      "$ref": "../common/GtsSessionId.json",
      "description": "Session identifier to search within"
    },
    "query": {
      "type": "string",
      "minLength": 1,
      "description": "Search query string"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "description": "Maximum number of results to return"
    },
    "offset": {
      "type": "integer",
      "minimum": 0,
      "description": "Offset for pagination"
    }
  },
  "required": ["session_id", "query"],
  "additionalProperties": false
}
