{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gts://gts.cyberfabric.chat_engine.common.retention_policy.v1~",
  "title": "Retention Policy",
  "description": "Data retention and lifecycle policy configuration",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Enable retention policy enforcement"
    },
    "active_retention_days": {
      "type": "integer",
      "minimum": 1,
      "maximum": 3650,
      "description": "Days to retain active sessions before auto-archive"
    },
    "archive_after_inactive_days": {
      "type": "integer",
      "minimum": 1,
      "maximum": 365,
      "description": "Days of inactivity before archival"
    },
    "soft_delete_retention_days": {
      "type": "integer",
      "minimum": 1,
      "maximum": 365,
      "description": "Days to keep in soft-deleted state before hard delete"
    },
    "allow_user_deletion": {
      "type": "boolean",
      "description": "Allow users to request immediate deletion"
    },
    "auto_cleanup_enabled": {
      "type": "boolean",
      "description": "Enable automatic cleanup of expired soft-deleted data"
    }
  },
  "required": ["enabled"],
  "additionalProperties": false
}
