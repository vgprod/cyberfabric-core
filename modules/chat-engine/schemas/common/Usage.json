{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gts://gtx.cf.chat_engine.common.usage.v1~",
  "title": "Usage",
  "description": "Backend processing metrics reported by the backend plugin for a message response",
  "type": "object",
  "properties": {
    "input_units": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of input processing units consumed by the backend"
    },
    "output_units": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of output processing units generated by the backend"
    },
    "total_units": {
      "type": "integer",
      "minimum": 0,
      "description": "Total processing units (input + output)"
    }
  },
  "required": ["input_units", "output_units", "total_units"],
  "additionalProperties": false
}
