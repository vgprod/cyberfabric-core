{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "gts://gtx.cf.core.events.event.v1~x.chat_engine.webhook.session_summary_event.v1~",
  "title": "SessionSummaryEvent",
  "description": "Webhook event sent to request session summarization",
  "type": "object",
  "properties": {
    "event": {
      "type": "string",
      "const": "session.summary",
      "description": "Event type identifier"
    },
    "session_id": {
      "$ref": "../common/GtsSessionId.json"
    },
    "enabled_capabilities": {
      "type": "array",
      "items": { "$ref": "../common/CapabilityValue.json" },
      "description": "Capability values active for this summarization"
    },
    "history": {
      "type": "array",
      "items": { "$ref": "../common/Message.json" },
      "description": "Session message history"
    },
    "summarization_settings": {
      "oneOf": [
        { "$ref": "../common/SummarizationSettings.json" },
        { "type": "null" }
      ],
      "description": "Optional summarization settings"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Event timestamp",
      "examples": ["2024-01-15T10:30:00Z"]
    }
  },
  "required": ["event", "session_id", "history", "timestamp"],
  "additionalProperties": false
}
